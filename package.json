{
  "name": "clouddown-editor-tooling",
  "version": "1.0.0",
  "private": true,
  "description": "Dev tooling (Markdown formatting via Prettier) for the CloudDown.Editor repo.",
  "scripts": {
    "format": "prettier --write \"**/*.md\"",
    "format:check": "prettier --check \"**/*.md\""
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
